version: "3.8"
services:
  # sudo chown -R 472:472 /server/grafana
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    cpus: 1
    mem_limit: 1G
    environment:
    - PGID=${PGID}
    - PUID=${PUID}
    - TZ=${TZ}
    - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
    - /server/grafana:/var/lib/grafana
    expose:
    - 3000
    labels:
    - com.centurylinklabs.watchtower.enable=true
    - diun.enable=true
    - diun.watch_repo=true
    # healthcheck:
    #   test: ["CMD-SHELL", "wget --quiet --tries=1 --spider https://grafana.davydehaas.nl/api/health || exit 1"]
    #   interval: 60s
    #   timeout: 10s
    #   retries: 2
