services:
  renovate:
    image: renovate/renovate:37.432.0@sha256:e3f41c965aa5b98ff593b751f1ea28d19406e03239736e7c2f81eb05b8509250
    container_name: renovate
    restart: unless-stopped
    cpus: 1
    mem_limit: 4096m
    environment:
      - PGID=${PGID}
      - PUID=${PUID}
      - TZ=${TZ}
      - RENOVATE_TOKEN=${RENOVATE_TOKEN}
      - RENOVATE_EXECUTION_TIMEOUT=300
      - RENOVATE_AUTODISCOVER=false
      - RENOVATE_GIT_AUTHOR=Renovate Bot <bot@renovateapp.com>
      - RENOVATE_PLATFORM=github
      - RENOVATE_REPOSITORIES=davydehaas98/home-server,davydehaas98/media-server
      - LOG_LEVEL=DEBUG
    volumes:
      - ${USERDIR}/renovate/config.js:/usr/src/app/config.js
    labels:
      - diun.enable=true
    networks:
      - reverse-proxy
networks:
  reverse-proxy:
    external: true
    name: reverse-proxy_default
