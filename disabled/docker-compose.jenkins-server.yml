services:
  # Open source automation server
  jenkins-server:
    image: jenkins/jenkins:2.387.3-lts-jdk11@sha256:2f47c4f27526dcf8c76b269ff1b98d78df47d7c5370ea133fe371f156abf0f59
    container_name: jenkins-server
    restart: unless-stopped
    environment:
    - PGID=${PGID}
    - PUID=${PUID}
    - TZ=${TZ}
    - JAVA_OPTS=-Xmx4096m # Set maximum heap size to 4096 MB
    - JENKINS_OPTS=--handlerCountMax=300
    volumes:
    - ${USER_DIR}/jenkins:/var/jenkins_home
    - /var/run/docker.sock:/var/run/docker.sock
    expose:
    - 8080 # Web Interface
    ports:
    - 50000:50000 # Remote Java (JIRA) API
    labels:
    - com.centurylinklabs.watchtower.enable=true
    - diun.enable=true
    - diun.watch_repo=true