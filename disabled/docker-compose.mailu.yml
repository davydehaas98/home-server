# This file is auto-generated by the Mailu configuration wizard.
# Please read the documentation before attempting any change.
# Generated for compose flavor

version: '2.2'

services:

  # External dependencies
  redis:
    image: redis:alpine
    restart: always
    volumes:
    - "${USER_DIR}/mailu/redis:/data"
    
  # Core services
  front:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}nginx:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    logging:
      driver: json-file
    expose:
    - 80
    - 443
    - 25
    - 465
    - 587
    - 110
    - 995
    - 143
    - 993
    volumes:
    - "${USER_DIR}/mailu/certs:/certs"
    - "${USER_DIR}/mailu/overrides/nginx:/overrides"


  admin:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}admin:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/data:/data"
    - "${USER_DIR}/mailu/dkim:/dkim"
    depends_on:
    - redis

  imap:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}dovecot:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/mail:/mail"
    - "${USER_DIR}/mailu/overrides:/overrides"
    depends_on:
    - front

  smtp:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postfix:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/overrides:/overrides"
    depends_on:
    - front

  antispam:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}rspamd:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/filter:/var/lib/rspamd"
    - "${USER_DIR}/mailu/dkim:/dkim"
    - "${USER_DIR}/mailu/overrides/rspamd:/etc/rspamd/override.d"
    depends_on:
    - front

  # Optional services
  antivirus:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}clamav:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/filter:/data"

  webdav:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}radicale:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/dav:/data"

  fetchmail:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}fetchmail:${MAILU_VERSION:-1.7}
    restart: always
    env_file: mailu.env

  # Webmail
  webmail:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}roundcube:${MAILU_VERSION:-1.6}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/webmail:/data"
    depends_on:
    - imap

  database:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postgresql:${MAILU_VERSION:-1.6}
    restart: always
    env_file: mailu.env
    volumes:
    - "${USER_DIR}/mailu/data/psql_db:/data"
    - "${USER_DIR}/mailu/data/psql_backup:/backup"

networks:
  default:
    driver: bridge
    ipam:
      driver: default
      config:
      - subnet: 192.168.203.0/24
