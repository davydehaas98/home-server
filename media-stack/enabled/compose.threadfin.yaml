networks:
  reverse-proxy:
    external: true

services:
  # xTeve M3U Proxy for Plex DVR and Emby Live TV
  # https://iptv-4k.co/shop/
  # https://uw8k.cc/
  threadfin:
    image: docker.io/fyb3roptik/threadfin:1.3.0
    container_name: threadfin
    user: ${UID}:${GID}
    networks:
      - reverse-proxy
    volumes:
      - ~/home-server/threadfin/config:/home/threadfin/conf:rw
      - ~/home-server/threadfin/config:/tmp/threadfin:rw
    environment:
      PGID: ${GID}
      PUID: ${UID}
      TZ: ${TZ}
    labels:
      traefik.enable: true
      traefik.http.routers.threadfin.entrypoints: websecure
      traefik.http.routers.threadfin.rule: Host(`threadfin.${DOMAIN_NAME}`) || Host(`dvr.${DOMAIN_NAME}`) || Host(`xteve.${DOMAIN_NAME}`)
      traefik.http.routers.threadfin.middlewares: chain-authentik@file
      traefik.http.services.threadfin.loadbalancer.server.port: 34400
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://threadfin:34400/web/"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      restart_policy:
        condition: unless-stopped
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.1'
          memory: 256M
