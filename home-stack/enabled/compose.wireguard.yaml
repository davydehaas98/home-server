networks:
  reverse-proxy:
    external: true

services:
  wireguard:
    image: lscr.io/linuxserver/wireguard:1.0.20210914@sha256:84c9fef8070216cf2c834e90af62e4a45226aa64e9fb0a3d3b9d13bceaaf8f34
    container_name: wireguard
    restart: unless-stopped
    cpus: 1
    mem_limit: 512m
    user: ${GID}:${UID}
    cap_add:
      - NET_ADMIN
    environment:
      - TZ=${TZ}
      - PUID=${UID}
      - PGID=${GID}
    volumes:
      - ~/home-server/wireguard/config:/config
    ports:
      - 51820:51820/udp
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
    networks:
      - reverse-proxy
