networks:
  reverse-proxy:
    external: true

services:
  omni-tools:
    image: docker.io/iib0011/omni-tools:0.6.0
    container_name: omni-tools
    networks:
      - reverse-proxy
    labels:
      traefik.enable: true
      traefik.http.routers.omni-tools.entrypoints: websecure
      traefik.http.routers.omni-tools.rule: Host(`omni-tools.${DOMAIN_NAME}`)
      traefik.http.routers.omni-tools.middlewares: chain-authentik@file
      traefik.http.services.omni-tools.loadbalancer.server.port: 80
    healthcheck:
      test: ["CMD", "curl", "--fail", "localhost:80/healthz"]
    deploy:
      restart_policy:
        condition: unless-stopped
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.1'
          memory: 128M
